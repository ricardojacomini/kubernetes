# roles/kubernetes/templates/kubelet.conf.j2

{% set base = kubelet.base_flags | default([]) %}
{% set gpu = kubelet.gpu_flags if 'gpu' in group_names else [] %}
KUBELET_EXTRA_ARGS="{{ (base + gpu) | join(' ') }}"
